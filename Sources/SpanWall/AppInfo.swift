import Foundation

/// Single source of truth for the app's identity.
/// Change `displayName` here (plus the 3 spots noted in Package.swift) to rename.
enum AppInfo {
    static let displayName = "SpanWall"
    static let bundleIdentifier = "com.nicholas.spanwall"
    static let phase = "Fase 2"
}
